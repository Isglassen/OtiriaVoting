{
  "version": "0.0.2",
  "dependencies": {
    "discord.js": "^14.8.0",
    "mysql": "^2.18.1",
    "typescript": "^5.0.2"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.21"
  },
  "type": "commonjs",
  "scripts": {
    "start": "npm install && npm run build && npm run run",
    "build": "npx tsc",
    "run": "node .",
    "deploy": "npm run build && node ./compiled/deployCommands.js",
    "deployStart": "npm install && npm run deploy && npm run run"
  }
}
